// find vowels from string in javascript
// const findVowels = (str) => {
//     const vowels = 'aeiouAEIOU'; // Define the vowels (both lowercase and uppercase)
//     const result = []; // Initialize an array to store the vowels found
    
//     // Loop through each character in the string
//     for (let i = 0; i < str.length; i++) {
//         if (vowels.includes(str[i])) { // Check if the current character is a vowel
//             result.push(str[i]); // Add the vowel to the result array
//         }
//     }
    
//     return result; // Return the array of vowels found
// };


// Test the function
const string = "Hello World!";
console.log(findVowels(string)); // Output: ['e', 'o', 'o']
